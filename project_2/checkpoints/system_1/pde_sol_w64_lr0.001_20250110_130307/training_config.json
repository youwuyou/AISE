{
    "model_config": {
        "width": 64,
        "activation_fun": "Tanh",
        "device": "cuda"
    },
    "training_config": {
        "learning_rate": 0.001,
        "num_epochs": 300,
        "batch_size": 64,
        "weight_decay": 0.0001
    }
}